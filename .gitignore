# Ignorar dependencias de Node.js
# Esta carpeta se regenera automáticamente al ejecutar "npm install"
/node_modules

# Ignorar archivos de configuración con información sensible
# Este archivo probablemente contiene tus claves de API de Firebase.
# Es una mala práctica subirlo a un repositorio.
# Se recomienda crear un archivo de ejemplo (ej: firebase-config.js.example)
# con la estructura necesaria, pero sin las claves reales.
firebase-config.js

# Ignorar archivos del sistema operativo
.DS_Store
Thumbs.db

# Ignorar variables de entorno
# Si en el futuro agregas un archivo .env para guardar variables,
# es fundamental ignorarlo.
.env
.env.*
!.env.example